/**
 * AVENTA browser extension — service worker (Manifest V3)
 * Optional: use for logging or future features.
 */
chrome.runtime.onInstalled.addListener(function () {
  // Extension installed or updated
});
