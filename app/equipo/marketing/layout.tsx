import MarketingHubShell from './components/MarketingHubShell';

export default function EquipoMarketingLayout({ children }: { children: React.ReactNode }) {
  return <MarketingHubShell>{children}</MarketingHubShell>;
}
