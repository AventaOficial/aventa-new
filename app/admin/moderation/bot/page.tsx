import ModerationFocusWorkspace from '@/app/components/moderation/ModerationFocusWorkspace';

export default function AdminModerationBotPage() {
  return <ModerationFocusWorkspace mode="admin" sourceTab="bot" />;
}
