import ModerationFocusWorkspace from '@/app/components/moderation/ModerationFocusWorkspace';

export default function AdminModerationUsersPage() {
  return <ModerationFocusWorkspace mode="admin" sourceTab="users" />;
}
